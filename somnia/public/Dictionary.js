function GetTranslation(component, language){
    switch(language.toUpper()){
        case 'EN':
            switch(component.toUpper()){
                case 'HEADER':
                    return {
                        
                    }

            }
            break;

        case 'HU':
            switch(component.toUpper()){

            }
            break;

        default:
            return null;
    }
}