//this function in the future will connect to database and retrieve
//all collections
export function GetCollections() {
  let collections = [];
  collections.push("season I");
  collections.push("season II");

  return collections;
}

//here, we'll get the items of collection
export function GetCollectionItems(collection) {
  return null;
}
